Code written in rust, make sure rust is installed.

How to run:
- move to vegsim directory
"cd vegsim"
- run in release mode (recommended)
cargo run -r 


Some parameters can be changed in UI
For more/all parameters see "vegsim/src/parameters.rs"